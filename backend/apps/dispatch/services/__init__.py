"""Dispatch services, one module per job.

    assign      who goes where
    routing     which way, and what is blocking it
    progress    a unit accepting, arriving, finishing
    reporting   read-only views: KPIs, why-this-unit, full state
"""
