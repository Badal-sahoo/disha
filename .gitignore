# The spec pack -- read it, don't commit it.
ps05

# The generated function map -- a reading aid, regenerated on demand.
docs

# Internal work split -- team-private, not part of the source.
TEAM.txt

# --- python / django ---
__pycache__/
*.py[cod]
.venv/
venv/
*.sqlite3
backend/media/
backend/staticfiles/

# --- node / vite ---
node_modules/
frontend/dist/
frontend/.vite/
npm-debug.log*

# --- secrets. .env.example IS committed; .env never is. ---
.env
.env.local
!.env.example
.env.example

# --- os / editor ---
.DS_Store
.idea/
.vscode/
*.swp
dishavenv